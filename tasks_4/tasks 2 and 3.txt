2. Сервис построения семейного дерева. Аналогов не встречал, конечное дерево строится и по женской и по мужской линям.
3. Посты - отдельная таблица, где колонки - id поста - ключ, id user, id сообщества, возможно есть смысл разделить посты на посты юзеров и посты сообществ.
	т.к. пост может содержать медиа то одна или несколько колонок id медиа, в зависимоти от того все медиа у нас хранятся в одной таблице или нескольких. 
	Я сторонник разделения медиа на несколько таблиц, в зависимости от типа медиа - аудио, видео, фото и т.д.
	Плюс необходимы колонки дата создания, дата последнего редактирования, колонка видимости - булево влияет на то пост будет отображатся или нет.
	Замечал что есть посты находящиеся постоянно в топе ленты, для этого нужен какой то признак, 
	например посты сортируются(по убыванию) по некоторой дате, например дата создания. 
	Создаем колонку - дата поста, которая по умолчанию ставится как дата создания но может быть изменена, ставим 01.01.2131 и пост постоянно находится сверху.
    Лайки - отдельная таблица, где колонки - id поста, id user, колонка like - булево где true это лайк false дизлайк. Ключ - id поста + id user
